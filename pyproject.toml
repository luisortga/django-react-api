[project]
name = "django-react"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "luisOrtga", email = "ortegaluiskb@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1.1",
]

[project.scripts]
django-react = "django_react:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
